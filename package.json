{
  "name": "netlify-plugin-contextual-site-env",
  "version": "0.2.0",
  "description": "A Netlify plugin to override ENV vars based on a site name",
  "main": "src/index",
  "repository": "https://github.com/eladhayun/netlify-plugin-contextual-env",
  "bugs": {
    "url": "https://github.com/eladhayun/netlify-plugin-contextual-env/issues"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "author": "Elad Hayun",
  "license": "MIT",
  "private": false,
  "keywords": [
    "netlify",
    "netlify-plugin",
    "env"
  ],
  "scripts": {
    "version": "auto-changelog -p --template keepachangelog && git add CHANGELOG.md",
    "prepublishOnly": "git push && git push --tags && gh-release",
    "test": "jest"
  },
  "devDependencies": {
    "auto-changelog": "^2.0.0",
    "gh-release": "^3.5.0",
    "jest": "^25.3.0"
  }
}